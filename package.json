{
  "name": "leer-official-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "りあ / Leer — AI CREATOR 公式サイト",
  "scripts": {
    "dev": "python3 -m http.server 8940 --bind 127.0.0.1 --directory public",
    "build": "node scripts/build-content.mjs",
    "test": "node scripts/build-content.mjs --check && node scripts/verify-site.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
