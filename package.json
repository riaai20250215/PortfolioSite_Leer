{
  "name": "leer-official-site",
  "private": true,
  "version": "1.0.0",
  "description": "りあ / Leer — AI CREATOR 公式サイト",
  "scripts": {
    "dev": "python3 -m http.server 8931 --bind 127.0.0.1 --directory public",
    "test": "node scripts/verify-site.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
